# Guardian Co-Pilot - an explainable driver-safety layer
# Verified working on Windows 11, Python 3.14, NVIDIA RTX 4070 Laptop (8 GB).
#
# ---------------------------------------------------------------------------
# GPU NOTE (important): the plain `pip install torch` gives a CPU-only build.
# For CUDA, install torch/torchvision FIRST from the PyTorch index, then the
# rest of this file:
#
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
#     pip install -r requirements.txt
#
# Everything still runs on CPU without CUDA — only slower (we measured a 38x
# GPU speed-up on this machine).
# ---------------------------------------------------------------------------

# --- The dataset toolkit needs these -------------------------------------
opencv-python>=4.8
numpy>=1.24
pandas>=2.0
matplotlib>=3.7

# --- Challenge 1: obstacle detection ---------------------------------------
# YOLOv8. LICENCE: AGPL-3.0 (see README "Licences").
ultralytics>=8.3

# --- Challenge 2: driver monitoring ----------------------------------------
# Face landmarks + blendshapes. LICENCE: Apache-2.0.
# Uses the Tasks API; the face_landmarker.task model is downloaded on first run.
mediapipe>=0.10.20

# --- Modelling / evaluation -------------------------------------------------
scikit-learn>=1.4
joblib>=1.3

# --- Deep-learning ablation (documented in the write-up, not shipped) -------
# Only needed to reproduce the CNN experiment that we report as a failure.
torch>=2.2
torchvision>=0.17

# --- Development ------------------------------------------------------------
pytest>=8.0
jupyter>=1.0

# ---------------------------------------------------------------------------
# Exact versions used to produce the submitted CSVs:
#   torch==2.13.0+cu130      torchvision==0.28.0+cu130
#   ultralytics==8.4.104     mediapipe==0.10.35
#   opencv-python==5.0.0.93  numpy==2.4.3
#   pandas==3.0.1            scikit-learn==1.8.0
#   joblib==1.5.3            matplotlib==3.11.1
# ---------------------------------------------------------------------------
